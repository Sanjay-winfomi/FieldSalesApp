// Shared transition timings — kept short/subtle per the design brief (avoid
// excessive motion).
export const transitions = {
  fast: '120ms ease',
  base: '180ms ease',
  slow: '260ms ease',
};

export const easing = {
  standard: 'cubic-bezier(0.4, 0, 0.2, 1)',
};
