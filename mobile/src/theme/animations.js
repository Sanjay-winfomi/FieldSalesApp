// Shared animation timings/easings so motion feels consistent, not tuned
// per-screen. Kept subtle — fade + slight rise, no bounce/overshoot.
export const durations = {
  fast: 150,
  base: 220,
  slow: 320,
};

export const pressScale = 0.97;
